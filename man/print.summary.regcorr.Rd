% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s3-print.R
\name{print.summary.regcorr}
\alias{print.summary.regcorr}
\title{Print a Summary of a Correlation Regression Model}
\usage{
\method{print}{summary.regcorr}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"summary.regcorr"}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
The argument \code{x}, returned invisibly.
}
\description{
Prints a \code{summary.regcorr} object produced by
\code{\link{summary.regcorr}}, including the model type, link function,
bootstrap standard errors, and the coefficient table with z-statistics
and p-values.
}
\examples{
set.seed(123)
n <- 100
x <- runif(n)
rho <- plogis(0.5 + 0.2 * x)
z1 <- rnorm(n)
y1 <- z1
y2 <- z1 * rho + rnorm(n) * sqrt(1 - rho^2)
fit <- regcorr(cbind(y1, y2) ~ x, data = data.frame(y1, y2, x), nboot = 20)
print(summary(fit))

}
\seealso{
\code{\link{summary.regcorr}} to create the summary;
  \code{\link{print.regcorr}} to print the fitted model itself.
}
